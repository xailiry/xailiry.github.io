# xailiry portfolio

Static bilingual portfolio website for `xailiry`.

## Local preview

Run any static file server from the project directory:

```powershell
python -m http.server 8000
```

Then open `http://localhost:8000`.

## Deployment

The project has no build step. Deploy the repository root as a static site to
Cloudflare Pages, Render Static Sites or GitHub Pages.
